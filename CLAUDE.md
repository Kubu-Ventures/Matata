# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This repo root is just a wrapper directory. The actual project — a Next.js PWA — lives entirely in `matata-app/`. All commands, architecture notes, and conventions are documented there:

@matata-app/CLAUDE.md

Always `cd matata-app` (or run commands with that as the working directory) before running `npm` scripts — there is no root-level `package.json`.
